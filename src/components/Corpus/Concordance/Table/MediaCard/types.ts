export type PostLinkProps = {
  media: string;
  docId: string;
  board: string;
};

export type MediaCardProps = {
  author: string;
  board: string;
  docId: string;
  media: string;
  year: string;
  title: string;
  dataNumber: number;
};
